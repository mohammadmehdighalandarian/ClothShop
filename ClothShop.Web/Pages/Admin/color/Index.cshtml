@page
@using ClothShop.Core.Convertors
@model ClothShop.Web.Pages.Admin.color.IndexModel
@{
    ViewData["Title"] = "لیست رنگ ها";
}

<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">لیست رنگ ها</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                لیست رنگ های سایت
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="table-responsive">
                    <div id="dataTables-example_wrapper" class="dataTables_wrapper form-inline" role="grid">
                        <div class="col-md-12" style="margin: 10px 0;">

                            <a asp-page="AddColor" class="btn btn-outline btn-success">افزودن رنگ جدید</a>

                        </div>
                        <table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables-example" aria-describedby="dataTables-example_info">
                            <thead>
                                <tr>
                                    <th>عنوان رنگ</th>
                                    <th>دستورات</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var color in Model.colorList)
                                {
                                    <form method="post">
                                    <tr>
                                        <td>@color.name</td>
                                        <td>
                                            <a href="/Admin/Color/EditColor/@color.id" class="btn btn-warning btn-sm">
                                                ویرایش
                                            </a>
                                                @if (color.isActive)
                                                {

                                                <button asp-page-handler="DeActive" type="submit" class="btn btn-danger btn-sm" value="@color.id" name="id">
                                                    غیر فعال سازی
                                                </button>

                                                }
                                                else
                                                {

                                                <button asp-page-handler="Active" type="submit" class="btn btn-success btn-sm" value="@color.id" name="id">
                                                    فعال سازی
                                                </button>

                                                }

                                        </td>
                                    </tr>
                                    </form>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>

            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>